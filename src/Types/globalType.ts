export type TService = {
  title: string;
  desc: string;
  bgColor: string;
};
export type TOrganizationalService = {
  title: string;
  bgColor: string;
};

export type TApproach = {
  title: string;
  desc: string;
};

export type TFounder = {
  name: string;
  designation: string;
  image: string;
};
